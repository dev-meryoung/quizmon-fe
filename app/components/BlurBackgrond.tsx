'use client';

import styles from 'app/styles/blurBackground.module.scss';

const BlurBackground = (): React.ReactNode => {
  return <div className={styles.wrapper} />;
};

export default BlurBackground;
